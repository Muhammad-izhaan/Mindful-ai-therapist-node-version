<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mindfullness | AI Therapeutic Companion</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@200;300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --color-bg-primary: #0C1B2E;
            --color-bg-secondary: #1A3A5F;
            --color-bg-tertiary: #2A1E3F;
            --color-text-primary: #E2E8F0;
            --color-text-secondary: #A0AEC0;
            --color-accent-primary: #3B82F6;
            --color-accent-secondary: #2563EB;
            --shadow-elegant: 0 10px 25px rgba(0, 0, 0, 0.2);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(-45deg, #0C1B2E, #2A1E3F, #1A3A5F, #2A1E3F);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            color: var(--color-text-primary);
            line-height: 1.6;
            perspective: 1000px;
            overflow-x: hidden;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Navigation Styles */
        .navbar {
            position: sticky;
            top: 0;
            left: 0;
            width: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 5%;
            background: rgba(12,27,46,0.8);
            backdrop-filter: blur(10px);
            z-index: 1000;
            transition: all 0.3s ease;
        }

        .navbar-logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--color-accent-primary);
            text-decoration: none;
            display: flex;
            align-items: center;
        }

        .navbar-logo span {
            margin-left: 10px;
            font-size: 0.7rem;
            color: var(--color-text-secondary);
            background-color: rgba(15,32,65,0.5);
            padding: 3px 8px;
            border-radius: 20px;
        }

        .navbar-links {
            display: flex;
            gap: 30px;
            margin-left: auto;
        }

        .navbar-links a {
            color: var(--color-text-secondary);
            text-decoration: none;
            transition: all 0.3s ease;
            position: relative;
        }

        .navbar-links a:hover {
            color: var(--color-accent-primary);
        }

        .navbar-links a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -5px;
            left: 0;
            background-color: var(--color-accent-primary);
            transition: width 0.3s ease;
        }

        .navbar-links a:hover::after {
            width: 100%;
        }

        /* Hero Section with 3D Effects */
        .hero {
            min-height: 100vh;
            padding: 2rem 5%;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 2rem;
            perspective: 2000px;
        }

        .hero-content {
            flex: 1;
            max-width: 600px;
            background: linear-gradient(135deg, rgba(26, 32, 44, 0.8), rgba(42, 30, 63, 0.6));
            padding: 2.5rem;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
            transform: translateZ(50px);
            transition: transform 0.5s ease;
        }

        .hero-content:hover {
            transform: translateZ(70px);
        }

        .btn {
            display: inline-flex;
            align-items: center;
            padding: 12px 25px;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            transform-style: preserve-3d;
            position: relative;
            overflow: hidden;
        }

        .btn i {
            margin-right: 8px;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--color-accent-primary), var(--color-accent-secondary));
            color: white;
            transform: translateZ(20px);
            box-shadow: 0 5px 15px rgba(59, 130, 246, 0.4);
        }

        .btn-primary:hover {
            transform: translateZ(30px) translateY(-5px);
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.6);
        }

        .btn-secondary {
            background: rgba(59, 130, 246, 0.1);
            color: var(--color-accent-primary);
            border: 2px solid var(--color-accent-primary);
            transform: translateZ(15px);
        }

        .btn-secondary:hover {
            transform: translateZ(25px) translateY(-5px);
            background: rgba(59, 130, 246, 0.2);
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.3);
        }

        .cta-buttons {
            display: flex;
            gap: 20px;
            margin-top: 30px;
            transform-style: preserve-3d;
        }

        .illustration-section {
            flex: 1;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            transform-style: preserve-3d;
            perspective: 1000px;
        }

        .hero-image {
            position: relative;
            transform-style: preserve-3d;
            transition: transform 0.6s ease;
        }

        .hero-image img {
            max-width: 900px;
            width: 100%;
            height: auto;
            transform: rotateY(-10deg) rotateX(5deg);
            transition: transform 0.6s ease;
            filter: drop-shadow(0 30px 60px rgba(0, 0, 0, 0.4));
        }

        .hero-image:hover img {
            transform: rotateY(-5deg) rotateX(2deg) scale(1.05);
            filter: drop-shadow(0 40px 80px rgba(0, 0, 0, 0.5));
        }

        /* Mobile Styles */
        @media (max-width: 768px) {
            .navbar {
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                padding: 1.2rem;
                background: rgba(12,27,46,0.9);
                backdrop-filter: blur(10px);
                z-index: 1000;
                display: flex;
                justify-content: space-between;
                align-items: center;
            }

            .navbar-logo span {
                font-size: 0.7rem;
                margin-left: 0.5rem;
                background: rgba(15,32,65,0.5);
                padding: 2px 8px;
                border-radius: 20px;
            }

            .navbar-links {
                display: none;
            }

            .hero {
                flex-direction: column;
                padding-top: 6rem;
                min-height: auto;
                text-align: center;
            }

            .hero-content {
                margin: 2rem auto;
                width: 90%;
                transform: none;
            }

            .cta-buttons {
                justify-content: center;
            }

            .illustration-section {
                display: none;
            }

            .mobile-illustration-section {
                display: block;
                padding: 1rem;
                margin: 1rem auto;
                width: 95%;
                max-width: 800px;
                transform-style: preserve-3d;
                perspective: 1000px;
            }

            .mobile-illustration-section .hero-image {
                transform-style: preserve-3d;
            }

            .mobile-illustration-section img {
                width: 100%;
                height: auto;
                transform: rotateY(-10deg) rotateX(5deg);
                transition: transform 0.6s ease;
                filter: drop-shadow(0 30px 60px rgba(0, 0, 0, 0.4));
            }

            .mobile-illustration-section .hero-image:hover img {
                transform: rotateY(-5deg) rotateX(2deg) scale(1.05);
                filter: drop-shadow(0 40px 80px rgba(0, 0, 0, 0.5));
            }
        }

        /* Desktop Styles */
        @media (min-width: 769px) {
            .mobile-illustration-section {
                display: none;
            }
        }

        /* 3D Background Effects */
        .bg-shapes {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
            overflow: hidden;
        }

        .bg-shape {
            position: absolute;
            background: rgba(59,130,246,0.1);
            border-radius: 50%;
            animation: float 10s infinite alternate;
        }

        @keyframes float {
            0% { transform: translateY(0) rotate(0deg); }
            100% { transform: translateY(-50px) rotate(360deg); }
        }

        /* Hamburger Menu for Mobile */
        .menu-toggle {
            display: none;
            flex-direction: column;
            cursor: pointer;
        }

        .menu-toggle .hamburger {
            height: 3px;
            width: 25px;
            background: var(--color-text-primary);
            margin-bottom: 4px;
            transition: all 0.3s ease;
        }

        @media (max-width: 768px) {
            .menu-toggle {
                display: flex;
            }

            .navbar-links {
                margin-left: 0;
                gap: 20px;
            }

            .menu-toggle {
                position: fixed;
                top: 20px;
                left: 50%;
                transform: translateX(-50%);
                z-index: 1100;
            }
        }

        .illustration-section {
            padding: 100px 5%;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .illustration-container {
            max-width: 500px;
            margin: 0 auto;
        }

        .brain-illustration {
            width: 100%;
            height: 100%;
        }

        .about-section {
            background: rgba(42,30,63,0.5);
            padding: 50px 5%;
            text-align: center;
        }

        .about-content {
            max-width: 800px;
            margin: 0 auto;
        }

        .about-section h2 {
            font-size: 2.5rem;
            margin-bottom: 30px;
            background: linear-gradient(135deg, var(--color-accent-primary), var(--color-accent-secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .about-section p {
            color: var(--color-text-secondary);
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .social-links {
            display: flex;
            gap: 20px;
            margin-top: 30px;
            transform-style: preserve-3d;
            justify-content: center;
        }

        .social-link {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: rgba(59, 130, 246, 0.1);
            color: var(--color-text-primary);
            font-size: 1.5rem;
            text-decoration: none;
            transition: all 0.3s ease;
            transform: translateZ(20px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            position: relative;
            overflow: hidden;
        }

        .social-link:before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--color-accent-primary), var(--color-accent-secondary));
            opacity: 0;
            transition: opacity 0.3s ease;
            z-index: -1;
        }

        .social-link:hover {
            transform: translateZ(30px) translateY(-5px);
            color: white;
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.3);
        }

        .social-link:hover:before {
            opacity: 1;
        }

        @media (max-width: 768px) {
            .social-links {
                margin-top: 20px;
            }
        }

        .mobile-illustration-section {
            display: none;
        }

        @media (max-width: 768px) {
            .mobile-illustration-section {
                display: block;
                padding: 50px 5%;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <a href="/" class="navbar-logo">
            Mindfullness 
            <span>by Izhan</span>
        </a>
        <div class="navbar-links">
            <a href="/chat">Chat</a>
            <a href="#about">About</a>
            <a href="#contact">Contact</a>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1>Mindfullness AI</h1>
            <p>Your compassionate AI companion for mental wellness, developed by Izhan.</p>
            <p>Experience personalized, empathetic support at your fingertips.</p>
            <div class="cta-buttons">
                <a href="/chat" class="btn btn-primary">
                    <i class="fas fa-comments"></i> Start Chatting
                </a>
                <a href="#about" class="btn btn-secondary">
                    <i class="fas fa-info-circle"></i> Learn More
                </a>
            </div>
            <div class="social-links">
                <a href="https://linkedin.com/in/muhammad-izhan" class="social-link" target="_blank">
                    <i class="fab fa-linkedin-in"></i>
                </a>
                <a href="https://github.com/Muhammad-izhaan" class="social-link" target="_blank">
                    <i class="fab fa-github"></i>
                </a>
            </div>
        </div>
        <div class="illustration-section">
            <div class="hero-image">
                <img src="https://raw.githubusercontent.com/Muhammad-izhaan/Mindful-ai-therapist-node-version/refs/heads/main/public/images/brain-waves.svg" alt="Mindfullness Brain Waves">
            </div>
        </div>
    </section>

    <!-- Mobile-only Illustration -->
    <div class="mobile-illustration-section">
        <div class="hero-image">
            <img src="https://raw.githubusercontent.com/Muhammad-izhaan/Mindful-ai-therapist-node-version/refs/heads/main/public/images/brain-waves.svg" alt="Mindfullness Brain Waves">
        </div>
    </div>

    <!-- Illustration Section -->
    <section class="illustration-section">
        <div class="illustration-container">
            <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg" class="brain-illustration">
                <path d="M50 20 Q60 40, 50 60 Q40 80, 50 100" fill="none" stroke="rgba(61,90,128,0.3)" stroke-width="2"/>
                <path d="M30 30 Q40 50, 30 70" fill="none" stroke="rgba(61,90,128,0.2)" stroke-width="1.5"/>
                <path d="M70 30 Q60 50, 70 70" fill="none" stroke="rgba(61,90,128,0.2)" stroke-width="1.5"/>
                <circle cx="50" cy="50" r="5" fill="rgba(61,90,128,0.1)"/>
            </svg>
        </div>
    </section>

    <!-- About Section -->
    <section id="about" class="about-section">
        <div class="about-content">
            <h2>About Mindfullness</h2>
            <p>Mindfullness is an innovative AI-powered therapeutic companion designed to provide compassionate, personalized mental health support. Our mission is to leverage cutting-edge AI technology to create a supportive, understanding environment for mental wellness.</p>
            <p>Developed by Izhan, a passionate technologist dedicated to improving mental health accessibility through innovative solutions.</p>
            
            <div class="social-links">
                <a href="https://www.linkedin.com/in/muhammad-izhan-a404752a6/" target="_blank" class="social-link">
                    <i class="fab fa-linkedin"></i> LinkedIn
                </a>
                <a href="https://github.com/Muhammad-izhaan?tab=repositories" target="_blank" class="social-link">
                    <i class="fab fa-github"></i> GitHub
                </a>
            </div>
        </div>
    </section>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const menuToggle = document.getElementById('menu-toggle');
            const navbarLinks = document.getElementById('navbar-links');

            menuToggle.addEventListener('click', () => {
                menuToggle.classList.toggle('active');
                navbarLinks.classList.toggle('active');
            });

            // Close menu when clicking outside
            document.addEventListener('click', function(event) {
                if (!menuToggle.contains(event.target) && !navbarLinks.contains(event.target)) {
                    menuToggle.classList.remove('active');
                    navbarLinks.classList.remove('active');
                }
            });

            // Prevent menu close when clicking inside navbar links
            navbarLinks.addEventListener('click', function(event) {
                event.stopPropagation();
            });
        });

        // Optional 3D Hover Effects
        document.addEventListener('mousemove', (e) => {
            const heroSection = document.querySelector('.hero');
            const heroContent = document.querySelector('.hero-content');
            const heroImage = document.querySelector('.hero-image');

            const centerX = window.innerWidth / 2;
            const centerY = window.innerHeight / 2;

            const moveX = (e.clientX - centerX) / 50;
            const moveY = (e.clientY - centerY) / 50;

            heroContent.style.transform = `translateZ(50px) rotateX(${-moveY}deg) rotateY(${moveX}deg)`;
            heroImage.style.transform = `rotateX(${moveY}deg) rotateY(${-moveX}deg)`;
        });

        // Smooth scroll to about section
        document.querySelectorAll('a[href="#about"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector('#about').scrollIntoView({ 
                    behavior: 'smooth' 
                });
            });
        });
    </script>
</body>
</html>
